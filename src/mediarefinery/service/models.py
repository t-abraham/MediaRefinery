"""Pydantic models for request/response payloads.

(Distinct from ``service.scheduler``-managed model lifecycle, and from
the ML model catalog which is owned by ``service`` in Phase C.)
PR 1 stub only.
"""
