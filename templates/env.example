# Placeholders only. Copy to .env and fill locally; never commit .env.
# The Immich server URL is configured in config.yml at integration.immich.url.
IMMICH_API_KEY=REPLACE_WITH_IMMICH_API_KEY
MEDIAREFINERY_CONFIG=/config/config.yml
